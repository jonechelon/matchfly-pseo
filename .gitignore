# PYTHON & SYSTEM DEFAULTS
__pycache__/
*.py[cod]
*$py.class
.DS_Store
Thumbs.db

# VIRTUAL ENVIRONMENT & SECRETS (CRITICAL)
venv/
env/
.env
.env.*

# IDE SETTINGS
.vscode/
.idea/
.warp/

# LOGS & DEBUG
*.log
logs/
npm-debug.log*

# DATA FILES
# Raw CSVs (do not version)
data/*.csv

# Local database (do not version)
# Since we use Supabase, local JSON is just temporary/cache
data/*.json

# Exception: Required file for ANAC enrichment
!data/specificroutes_anac.json

# GENERATED HTML FILES (Auto-generated by GitHub Actions)
# Flight pages (generated from Python) — muitos arquivos, não versionar
docs/voo/*.html

# Destination pages (generated dynamically) — muitos arquivos, não versionar
docs/destino/*.html
# SECURITY (Keys & Tokens)
credentials/
service_account.json
client_secret.json


# BACKUPS & TEMPORARY FILES
projects/matchfly-pseo-backup-erros/
_internal_docs/archive/

# LEGACY (Cleanup)
# Ensures old public/ folder never gets pushed by mistake
public/


# NOTES
# The docs/ folder itself is NOT ignored - only generated content
# Static files like CNAME, .nojekyll, CSS, JS should be committed
# GitHub Actions regenerates all HTML files on each deployment
