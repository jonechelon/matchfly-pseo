# --- Python & System Defaults ---
__pycache__/
*.py[cod]
*$py.class
.DS_Store
Thumbs.db

# --- Virtual Environment & Secrets (CRÍTICO) ---
venv/
env/
.env
.env.*

# --- IDE Settings ---
.vscode/
.idea/
.cursor/

# --- Logs & Debug ---
*.log
npm-debug.log*

# --- DATA FILES ---
# CSVs brutos (não versionar)
data/*.csv

# Banco de dados Local (não versionar)
# Como usamos Supabase, o JSON local é apenas temporário/cache
data/*.json
# Exceção: arquivo necessário para enriquecimento ANAC
!data/specificroutes_anac.json

# --- SEGURANÇA (Chaves e Tokens) ---
credentials/
service_account.json
client_secret.json

# --- BACKUPS & LIXO ---
projects/matchfly-pseo-backup-erros/
_internal_docs/archive/

# --- LEGACY (Limpeza) ---
# Garante que a antiga pasta public nunca suba por engano
public/

# --- GITHUB PAGES ---
# A pasta docs/ NÃO está listada aqui propositalmente.
# Ela DEVE ser commitada para o site funcionar.